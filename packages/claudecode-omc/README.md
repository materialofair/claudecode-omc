# claudecode-omc

Claude Code distribution of curated OMC skills, agents, hooks, commands, and configuration.

```bash
npm install -g claudecode-omc
omc-manage setup
```

Use `omc-manage setup --harness opencode` for the legacy explicit OpenCode mode, or install `opencode-omc` for an OpenCode-first command.
