# @ah-wq/omc-core

Shared runtime and curated artifact bundle used by `claudecode-omc` and `opencode-omc`.

This is an implementation package. Install one of the harness-specific facade packages instead.
