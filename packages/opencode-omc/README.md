# opencode-omc

OpenCode-first distribution of curated OMC skills, agents, commands, configuration, and GLM defaults.

```bash
npm install -g opencode-omc
opencode-omc setup
```

The command defaults to the OpenCode harness, so no `--harness opencode` flag is required.
